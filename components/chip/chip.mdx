import { Canvas, Story, Subtitle, ArgsTable, PRIMARY_STORY } from '@storybook/addon-docs/blocks';
import DtChip from './chip';

# Chip

<Subtitle>
  A chip is a compact UI element that provides brief, descriptive information about an element.
  It is terse, ideally one word.
  It is important a button is identifiable, consistent, and communicates its actions clearly,
  and is appropriately sized to its action.
</Subtitle>

## Base Style

<Canvas>
  <Story id="components-chip--default" />
</Canvas>

## Slots, Props & Events

<ArgsTable story={PRIMARY_STORY} />

## Usage

Default behaviour is interactive, has hover/focus/active states.

Default content text is aria-label unless overridden by passed in aria-label.

```js
import { DtChip } from '@dialpad/dialtone-vue';
```

```html
<dt-chip
  :close-button-props="closeButtonProps"
  :aria-label="aria-label"
  :id="id"
  :content-class="test"
/>
```

## With Icon

<Canvas>
  <Story id="components-chip--with-icon" />
</Canvas>

```html
<dt-chip
  :close-button-props="closeButtonProps"
  icon: 'IconLock'
/>
```

## With Avatar

<Canvas>
  <Story id="components-chip--with-avatar" />
</Canvas>

```html
<dt-chip
  :close-button-props="closeButtonProps"
  avatar: "DP"
/>
```

## Non Interactive

Can't focus on the chip, can not tab through.

<Canvas>
  <Story id="components-chip--non-interactive" />
</Canvas>

```html
<dt-chip
  :close-button-props="closeButtonProps"
  interactive: false
  hideClose: true
/>
```
