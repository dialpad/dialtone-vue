name: Unit Tests

on:
  pull_request:
    branches: [ main, release ]
  push:
    branches: [ main ]

jobs:
  unit_tests:

    runs-on: ubuntu-latest

    needs: setup

    steps:

      - name: Run unit tests
        run: npm run test
